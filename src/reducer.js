// reducers.js

import _ from 'lodash'

let init = {};

export default function(state = init, action) {

    switch(action.type) {

        default:
            return state;
            
    }

};